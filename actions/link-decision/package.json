{
  "name": "knoledgr-link-decision",
  "version": "0.1.0",
  "description": "GitHub Action that enforces Knoledgr decision linkage on pull requests.",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "build": "ncc build src/index.js -o dist --license licenses.txt"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  },
  "engines": {
    "node": ">=20"
  }
}
